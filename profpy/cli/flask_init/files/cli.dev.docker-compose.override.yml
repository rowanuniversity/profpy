version: "3.4"
services:
  web:
    build: 
      context: "."
      dockerfile: "./docker/dev.Dockerfile"
    environment: 
      instance: "PPRD"
      full_login: "{database_user}@pprd"
    volumes:
      - ./app/:/app